<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Liste des cartes!{% endblock %}</title>
        <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text></svg>">
        <link rel="stylesheet" href="{{ asset('assets/theme/bootstrap.min.css') }}"> {% block stylesheets %}{% endblock %}
		<link rel="stylesheet" href="{{ asset('assets/css/style.css') }}">
		<script defer src="{{ asset('assets/js/bootstrap.min.js') }}"></script>
    </head>
    <body class="fw-bolder">
		<header class="">
			<nav class="navbar navbar-expand-lg navbar-dark bg-primary">
				<div class="container-fluid">
					<a class="navbar-brand" href="{{ path('cardlist')}}">Carte Alexis |
					</a>
					<button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarColor01" aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
						<span class="navbar-toggler-icon"></span>
					</button>
					<div class="collapse navbar-collapse" id="navbarColor01">
						<ul class="navbar-nav ms-auto">
							
							<ul class="nav navbar-nav">
								{% if app.user %}
								{% if 'ROLE_ADMIN' in app.user.roles %}
									
									<li class="nav-item">
										<a class="nav-link " href="{{ path('card_add')}}">Ajouter une carte</a>
									</li>
									<li class="nav-item">
										<a class="nav-link " href="{{ path('app_logout')}}">Deconnexion</a>
									</li>
									<li class="nav-item">
										<a class="nav-link " href="{{ path('cardlist')}}">Accueil</a>
									</li>
								{% endif %}
								{% endif %}
									<li class="nav-item">
										<a class="nav-link " href="{{ path('cardlist')}}">Accueil</a>
									</li>
									<li>
										<a class="nav-link" href="{{ path('app_login') }}">Connexion</a>
									</li>	
							</ul>
						</ul>
					</div>
				</div>
			</nav>
		</header>
    <body>
        {% block body %}{% endblock %}
    </body>
</html>
